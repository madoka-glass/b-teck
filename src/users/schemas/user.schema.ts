import * as mongooses from 'mongoose'
export const UserSchema = new mongooses.Schema({
  username: {
    type: String,
    required: true,
    index: { unique: true }
  },
  password: {
    type: String,
    required: true
  }
})